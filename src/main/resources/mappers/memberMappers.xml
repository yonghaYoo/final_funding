<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.funding.mappers.memberMapper">		
	<cache />
	
	<select id="listMember" parameterType="int" resultType="MemberVO">
		select * from member
	</select>
	
<!-- 	<select id="infoMember" parameterType="int" resultType="MemberVO">
	select * from member where m_num=#{m_num}마이페이지
	</select>
	
	<select id="infoUpdate" parameterMap="int" resultMap="MemberVO">
	update Member set m_pw =#{m_pw}, m_email =#{m_email}, m_acc=#{m_acc}, m_add=#{m_add} 
	</select>
	<select id="Memberdelete" parameterMap="int">
	delete from Member where id =#{m_id}
	</select> -->
	
</mapper>